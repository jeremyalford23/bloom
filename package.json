{
  "name": "bloom-finance",
  "version": "0.5.1",
  "private": true,
  "description": "A private household financial decision engine.",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check public/app.js && node --test"
  },
  "engines": { "node": ">=24" }
}
